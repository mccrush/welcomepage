export function saveHref(data) {
  var dt = dt + data;
  var mydata = true;
  //var data = JSON.parse(localStorage.getItem(STORAGE_KEY_G) || '[]');
  console.log(data);
  return mydata;
}

export function saveTitle(data) {
  var dt = dt + data;
  var mydata = true;
  //var data = JSON.parse(localStorage.getItem(STORAGE_KEY_G) || '[]');
  console.log(data);
  return mydata;
}